from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='It classifies if a wafer is default or not',
    author='kritika',
    license='MIT',
)
